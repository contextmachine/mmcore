# Ledger — SSX v5 singular-cases: consolidated adversarial-review findings

**Date:** 2026-07-07
**Component:** `mmcore/numeric/intersection/ssx/_bez_ssx5.py`, `_ssx5_singular.py`
**Sources (two independent multi-agent reviews, reconciled here):**
- **[A]** background-session workflow (6 lens reviewers + refuting verifiers, pinned at `3b80072`): 14 confirmed majors, 0 refuted, 8 minors.
- **[B]** interactive-session review (24 agents, pinned at `7d47f68`): 18 confirmed findings C1–C18, 0 refuted — summarized in the project memory `project_ssx5_singular_cases.md` UPDATE section.

Line numbers refer to `3b80072`. Repro snippets are one-liner summaries; [A] items have full
repro + verifier evidence in the workflow result (session task `we2hgfi5c`). **None of these
items is sanctioned by the plan's AS-BUILT notes — all are real work items.** Coordination:
mark an item `CLAIMED(<who>)` before starting it; both sessions edit the same files.

## P0 — soundness / user-visible wrong topology

- [x] **L1. `BoxNet.excludes_zero` strict-comparison roundoff unsoundness** — `_ssx5_singular.py:188` — [A M4+M7+M11: three lenses independently] `min() > 0.0` with no roundoff margin: after a de Casteljau split exactly through a zero (any dyadic feature coordinate, s=0.5 ubiquitous — guided cuts deliberately pass through crossings), the restricted net's min drifts to ~+3e-17 and BOTH children of the solution-carrying box are excluded. Consequences observed: C1 cusp CURVE collapses to one isolated `cusp` with false-complete enumeration (cuspidal edge vs plane x=0); touch-owning cells knifed out. Fix: exclusion margin scaled to net magnitude (exclude only if `min > k·eps·max|c|`, symmetric for max<0) — margin makes exclusion STRICTER, stays sound. Audit the same pattern in `_definite`/`theorem3_excludes_c3`, `ShiftedPositiveNet`, the loop-free probe's hull gate, and Σ precheck in `c1_pass`.
  → FIXED `8a8f3ca`: shared `hull_excludes_zero`, K=128 (measured drift +2.776e-17 = eps/8 absolute on the M11 nets, constant down the zero-adjacent descent; margin 128·eps·max|c| = 256× that at the split); applied to BoxNet/VectorBoxNet/theorem3 `_definite`/c1_pass Σ precheck/loop-free probe gate; ShiftedPositiveNet audited strict-on-purpose (its `(atol·w_scale)²` shift ≫ eps·max|F_sq| by ~10 orders inside the O(1)–O(100) envelope). M11 now types `cusp_curve` with 35 samples spanning t∈[0,1] (no L14 gray zone; test `test_cusp_curve_on_split_plane_not_knifed_out`); coverage timings unchanged (4.13–8.12 s vs 4.45–8.35 reference). **M7 residual (= L4, since fixed at `868f42d`):** after L1 the loop-free probe's hull gate DOES fire on 2 touch-holding cells of the (0.3,0.3) touch+loop, but the center-only witness fails (`ok=False, 0 roots` — 0.3 is a box corner, center-GN lands elsewhere) and the arm `continue`d without subdivision fallback → touch still lost, ring found as 6 fragments (L10). Output identical pre/post L1 on M7 (verified with margin forced to 0) — the loss was 100% the L4 path. L4's fix also extended THIS item's audit list: `_aabb_disjoint` had the same strict-comparison drift unsoundness (it killed L4's probe descent at depth 8) and the probe arm's fresh-built Ψ-component hulls need a coordinate-scale margin (per-component max|c| shrinks below the absolute drift on touch-hugging nets).

- [x] **L2. Φ-slice fabricates phantom `tangential` branches from sub-tolerance valleys; subsumption then deletes the real touch** — `_bez_ssx5.py:2284` — [A M1 ↔ B C16] At `eps=1e-3` the touch-plus-loop valley floor (`eps²/4 = 2.5e-7`) passes the `0.01·atol` refinement acceptance; `sin_ang ≤ 1e-3` routes seeds to the Φ marcher; TWO 375-pt phantom `tangential` branches (arc ~0.59, up to 58·atol off the true SSI) ship, one passes through the touch → filter L3 deletes the genuinely-emitted tangent_point. Ground truth: {1 touch, 1 transversal ring r=0.0158}. Controls: eps=8e-3/5e-3 clean. Fix direction: a Φ-marched closed fragment must be validated as a TANGENT feature (TΨ residual along path, not just Ψ-validity), and/or valley seeds rejected by normal-angle at the REFINED point against surface-pair, not seed sin_ang alone.
  → FIXED `52f5bcd`: `_phi_closed_frag_normals_aligned` — every Φ-marched closed fragment must hold sin_ang ≤ 1e-3 (the pipeline's transversality bar) at EVERY kept sample; measured separation: phantom max sin_ang 2.56e-2 vs genuine tangent circle max 3.96e-7 (five decades around the bar). Validation failure stops the ranked-equation retry for that seed (phantom is geometry, not equation choice). eps=1e-3 now ships {1 tangent_point (0.5,0.5,0), closed rings r≈0.0158, 0 tangential}; controls 5e-3/8e-3 stay exactly clean (1 touch + 1 closed ring each). Tests: `test_touch_plus_loop_small_eps_no_phantom_tangential` (~33 s — phantoms still marched before rejection) + `test_touch_plus_loop_controls_stay_clean[...]`.

- [x] **L3. Post-assembly tangent_point subsumption is xyz-only (4·atol, no parametric guard)** — `_bez_ssx5.py:4352` — [A M2 ↔ B C14] Violates the module's own binding both-guards convention: a certified touch whose 4D preimage is ~1e5·ptol from the overlap/tangential branch's preimage (z-wall of 640·atol between them) is deleted on 3D proximity alone. Repro: skew ruled patch vs plane with u=0 isoline overlap + genuine touch at (0.8, 0.5); touch 3·atol from the polyline in xyz → `singularities == []`. Fix: require BOTH xyz ≤ 4·atol AND per-axis stuv proximity to the branch's stuv polyline (4·ptol box vs nearest stuv sample, using the branch's stored curve[0]).
  → FIXED `15c5193`: `_point_on_branch_both_guards` — xyz ≤ 4·atol point-to-segment AND per-axis stuv ≤ 2·unify_tol against the branch stuv interpolated at the SAME segment location (all xyz-close segments tested, not just the nearest). Skew-ruled touch now reported (1 tangent_point at (0.5,0.003,0), stuv (0.8,0.5,0.5,0.2515)); control c=0.05/0.8 unchanged. DISCOVERED while fixing: legacy boundary-overlap branches store corrupt other-surface stuv (e.g. v=0.5 where the true preimage is 1.0, ~1e5·atol off — the same broken 4-vs-2 overlap bookkeeping), which the param guard cannot judge against → segments failing a stored-stuv↔own-xyz self-check (2·atol on both surfaces; healthy branches measure 1.8e-13) fall back to the pre-L3 xyz-only subsumption, so the legacy tangential AND overlaps cases still end at 0 singularities. The corrupt-overlap-stuv bug itself is unfixed (belongs with the overlap machinery / 4-vs-2 gap); a param-far touch near a CORRUPT-stuv branch would still be wrongly subsumed. Test: `test_param_far_touch_near_overlap_not_subsumed`.

- [x] **L4. Loop-free arm: center-only witness + unconditional `continue` drops touch-owning cells** — `_bez_ssx5.py:3897` — CLAIMED(bg-session) — [A M8 ↔ B C3] When the touch is NOT lattice-pinned (e.g. r⁴−εr² centered at (0.3,0.3)), the coarse loop-free cell's center GN diverges/lands elsewhere, the arm continues without subdivision fallback, and ALL touches are lost (`singularities == []`). Fix: on witness failure (or non-convergence) in a cell whose hull gate fired, fall through to subdivision instead of continuing; bounded by the existing size gate.
  → FIXED `868f42d`: witness failure pushes a lean PROBE descent (`probe_only` cells: prunes → T-hull gate → coordinate-scale Ψ-component hull gate → center witness → 2×2 net-split, size-gate bounded; never traces, never CSX) instead of subdividing — guided-cut fall-through measured +1.2 s on case 5 (20 near-tangent witness failures × ~60 ms cut-face CSX) and degraded the off-lattice ring's arc coverage. The (0.3,0.3) touch now reported (xyz=(0.3,0.3,0), stuv=(0.3,0.3,0.4,0.4)); traced geometry bit-identical to baseline (worst ring gap 0.01858 unchanged); coverage cases all ≤1.11× (case5 4.58 s vs 4.14 reference / 4.06 same-day baseline). TWO MORE L1-pattern roundoff races found+fixed on the descent path: `_aabb_disjoint` had NO margin (touch cell's z-hull max drifts to −3e-17 after 8 splits vs the plane's exact z=[0,0] — every depth-8 touch-holding cell was AABB-pruned; now inflated by HULL_MARGIN_K·eps at coordinate scale) and the probe arm's Ψ-hull gate needs a COORDINATE-scale margin, not `hull_excludes_zero`'s per-component max|c| (measured G_z hull [−3.9e-4, −9.8e-17]: drift 1e-16 vs per-component margin 1.1e-17). NOTE `_prune_ssx_cell`'s top-level AABB kept margin-free deliberately (top-level nets are un-split user data — no drift). Residual: ring fragmentation + arc gaps up to ~19·atol are L10 (measured identical pre/post); test asserts touch + on-circle branches + ≥20/24 arc coverage, tighten when L10 lands.

- [x] **L5. Tangent curves traced via the loop-free path carry `kind='transversal'`** — `_bez_ssx5.py:3904` — CLAIMED(bg-session) — [A M13+M9 ↔ B C17] Non-strict monotone T-hulls (≥0 touching zero along the curve) certify loop-free; fragments trace fine but never get `tangential`, which (a) breaks the output contract, (b) blinds filter L3's kind-keyed subsumption → stray tangent_points on the curve survive. Repro: cylinder z=(2t−1)² on plane; also (t−0.5)²·(s−0.5) mixed case. Fix: a loop-free cell whose hull gate fired AND whose traced fragment has near-zero TΨ along it should tag `tangential` (measure TΨ residual along the polyline, same bar as L2's fix).
  → FIXED `b44921f`: tagging by measurement, but L2's normal-alignment bar is provably UNDECIDABLE here in both directions (measured): the genuine tangent line's Ψ-marched samples wander in the sub-tolerance valley to sin_ang 1.08e-2 (an order ABOVE the 1e-3 bar — "misaligned⇒transversal" loses the tag), while the eps=1e-3 touch-plus-loop ring is genuinely transversal yet aligned to 2·eps^1.5≈6e-5 at every sample ("all-aligned⇒tangential" flips the committed L2 test's ground truth — caught by the suite mid-implementation). Shipped criterion: `_fragment_on_tangent_locus` Δ-SNAP — every sample must GN onto Δ=Ψ∩TΨ (shared plain-float `_delta_float_gn`, ~0.1 ms/start, extracted from `_emit_offcurve_tangent_roots`) moving ≤2·atol xyz. Cylinder now ships 1 `tangential` branch + `singularities == []` (witness subsumed by the correctly-kinded branch); all 7 coverage cases keep pure transversal kinds at unchanged timings; eps=1e-3/5e-3/8e-3 controls clean. Residual: the mixed (t−0.5)²(s−0.5) geometry's tangent line is lost in ASSEMBLY (only ~2e-3 stubs near the degenerate X ship, measured identical pre/post — L10-family), so its kind is untestable; sub-2e-4-eps trap sheets sit below the float GN's 1e-8 stall acceptance (documented in `_delta_float_gn` — the feature is sub-7·atol there anyway).

- [x] **L6. 1-dim Δ-sets mishandled as isolated points** — CLAIMED(bg-session) — [A M14 ↔ B C4] Two faces: (i) crossing-less 1-dim tangent curve floods ~300 spurious `tangent_point`s (exhausted flag ignored by `_emit_tangent_roots` consumers — B C4); (ii) coplanar PARTIAL overlap emits a phantom interior tangent_point (paper Fig 8 classifies overlap interior as 2-dim C2 — A M14, `_bez_ssx5.py:3897`). Fix: when enumeration returns `exhausted=True` with many roots (or roots on a positive-dimensional set — collinearity/curve_flag-style test), emit NOTHING typed as points from that cell; defer to deflation/overlap machinery.
  → FIXED `fa222d8`: (i) `_tangency_witness` surfaces `exhausted` (4-tuple); both full-enumeration consumers suppress ALL point emission on the 1-dim signature `_delta_roots_curve_like` (>12 roots, or exhausted with >1 — c1_pass's curve_flag convention). Flood repro pinned off-lattice (the on-lattice (q−1/4)² ring was accidentally clean — its Φ∩L branch subsumed everything): tangent ring z=(r²−0.04)² about (0.3,0.3) emitted ~50 on-ring samples (top cell 16 deduped roots exhausted=True via the crossing-less arm + 33- and 17-point `_emit_offcurve_tangent_roots` floods in crossing-bearing descendants with NO Φ-tube), 4 survived subsumption; now 0 tangent_points, ring ships as one `tangential` branch. (ii) tested first per plan: (i) does NOT cover it (the plane pair routes through the LOOP-FREE arm, enumerate_all=False — no exhausted signal); fixed via `_overlap_region_boxes` (padded 4D AABBs of overlap-boundary-segment connected components, matching-ladder grouping) + emission skip at every site — plane_patch(0,2)×plane_patch(1,3) now `singularities == []` with the 4 overlap branches kept, and the L3 skew-ruled genuine-touch-near-overlap control still reports its touch (s=0.8 far outside the u=0 overlap box). Residuals (documented in the docstrings): the crossing-bearing offcurve arm DROPS suppressed samples (no subdivision fall-through exists there — the 1349× note); ≤12-point debris can still ship when an exhausted enumeration returns few roots over a sparse tube (subsumption filter is the backstop); overlap boxes are endpoint-AABB reconstructions (curved-boundary under-coverage / non-convex over-coverage / L3's corrupt legacy stuv all degrade toward pre-existing behaviors).

- [x] **L7. C3 pass structurally blind to S2-side doubles** — CLAIMED(bg-session) — `_ssx5_singular.py:680` — [A M5 ↔ B C8] System {R1(s,t)=R2(u,v), R1(p,q)=R2(u,v)} + (s,t)≠(p,q) guard only certifies S1-side double preimages. Umbrella passed as S2 (plane as S1) → self-intersection invisible. Fix: run the pass twice with roles swapped (second system {R2=R1, R2=R1}), or add the symmetric 6-var system; dedup across both.
  → FIXED `1090d87`: generalized `newton6(z0, Sa_h, Sb_h, guard_ptol)` runs BOTH role assignments per broadphase candidate; hits normalized to `{stuv, stuv_mate}` — primary = (s,t,u,v), mate differs in the DOUBLED side only ((p,q,u,v) S1-side / (s,t,u',v') S2-side; SSXSingularity docstring documents it) — and deduped as unordered preimage pairs. Measured: plane-as-S1 vs umbrella-as-S2 now 1 self_intersection at (0,0,0.5) with mate v differing 0.707 (was 0); the shipped S1-side test still exactly 1 (the mirror run's guard rejects the unique-preimage side — no phantom). Test: `test_self_intersection_point_s2_side`. Residual: a point where BOTH surfaces are 2-to-1 legitimately yields one hit per distinct preimage pair (up to 2 attributions of one 3D point — informative, not deduped away by design).

- [x] **L8. `c3_possible` per-cell Theorem-3 gate is semantically wrong for cross-cell C3** — CLAIMED(bg-session) — `_bez_ssx5.py:3901` — [A M6, mine only] Theorem 3 is per-box injectivity; a C3 whose two preimages lie in DIFFERENT traced cells passes every per-cell check and `c3_pass` never runs. Repro: figure-eight wall vs plane (both preimages S1-side, separate cells) → 0 self_intersections. Fix: c3_possible must consider PAIRS (any two traced cells whose xyz AABBs overlap and that aren't jointly certified), or simply always run the (broadphase-cheap) c3_pass when ≥2 branches/segments share xyz proximity.
  → FIXED `a80288f`: gate removed; c3_pass runs whenever collision is possible at all (≥2 branches OR a branch with ≥8 segments — the broadphase same-branch index gap ≥3). Repro note (measured, honest): building the ledger's exact figure-eight net against a plane the SSI only self-crosses for z0∈(0.65,0.69) — at z0=0.66 every branch-carrying traced cell CERTIFIES per-cell Theorem 3 (instrumented: 6/6 CERT — per-cell checks provably cannot see this C3), yet at HEAD the gate happened to fire anyway through two EMPTY interior bounce-cells inheriting corner crossings (subdivision-path luck, not semantics); the fix makes detection structural. Measured free-when-fired: coverage 7×100% at 4.34/7.57/3.17/1.39/1.79/7.41/8.34 s (≤1.0× of 4.74/7.87/3.28/1.43/1.88/7.59/8.53 reference), ZERO singularity lines; legacy 4 baseline cases 0 singularities (incl. overlaps' 4 always-run branches — corner solutions die on the same-preimage guard); case 10 broadphase 0 pairs/115 segs, ~0.3 ms. `theorem3_excludes_c3` kept exported with an UNWIRED docstring note (sound per-box certificate, invalid global gate). Tests: `test_cross_cell_self_intersections_figure_eight` (2 crossings at (0,0.3,0.66)/(0,0.9,0.66)), `test_theorem3_skips_regular_case` (comment updated). Residual: a SINGLE branch with <9 vertices never triggers the pass (a 4–8-segment self-crossing polyline is physically implausible under the 2·atol sagitta bar, but it is a documented trigger floor).

- [x] **L9. Rational inputs: TΨ minor nets built from dehomogenized control points are NOT the rational surface’s minors** — CLAIMED(bg-session) — `_bez_ssx5.py:3746` — [A M12, mine only] `P_cart = S[...,:-1]/S[...,-1:]` feeding `minors_Tpsi_from_control_nets` is wrong for non-uniform weights; the whole tangency chain inherits it → C2 tangency silently lost on rational input at O(1) scale (weight-2 rationalized paraboloid: no tangent_point, spurious micro-branch instead). NOTE `bez_ssx`'s signature default is `rational=True`. Fix: build TΨ from the homogeneous quotient-rule derivative nets (the `_deflate.py` machinery already has the homogeneous product primitives; same pattern as `sigma_normal_net`'s rational branch), or document loudly + gate rational inputs into the polynomial path only when weights≈1.
  → FIXED `51f7c4e`: TΨ minors built from the true rational Jacobian's quotient-rule NUMERATOR columns. For R = P/w, R_a = (P_a·w − P·w_a)/w², so each 3×3 minor of the true Jacobian equals the numerator-column minor ÷ a strictly positive power-of-W product (T1,T2 ÷ W1²W2⁴; T3,T4 ÷ W1⁴W2²; W>0 for valid weights) → SAME zero set + sign structure (all any consumer needs: hull gates, sign-definite certs, monotonicity, deflation zero-finding). New `_deflate.minors_from_column_nets` (factored out of `minors_Tpsi_from_control_nets`, which now calls it — BIT-IDENTICAL, np.array_equal), `_ssx5_singular.minors_Tpsi_rational` + `_rational_jacobian_numerator_columns` (numerator columns via `sigma_normal_net`'s exact same-param products). `bez_ssx` routes NON-uniform-weight rational input to the new path; unit weights keep the exact polynomial path — BIT-IDENTICAL to pre-L9 (cases 5/10 branch polylines np.array_equal vs HEAD; coverage 7×100% at ≤1.04× timings). **Math verified** (`test_rational_tpsi_minors_match_true_jacobian`): 1280/1280 sign agreement + exact positive-W ratio to 2.2e-12 rel over 40 random rational pairs × 8 samples. **Repro** (`test_rational_weights_tangent_point_found`): exactly-rationalized paraboloid (w 1..2, geometry unchanged) now finds tangent_point (0.5,0.5,0), branches=[], points=[] in 3.0 s (was: touch lost). Transversal rational twin traces the same branch (`test_rational_transversal_sanity_matches_polynomial`). **Cost:** rational T nets are higher-degree (numerator columns ~2× degree/axis, minors triple) — measured seconds on the repro, acceptable; fast path keeps all w≡1 work untouched. **Audit — DeflatedSystem/Ψ:** the witness (`_tangency_witness`/`_check_tangency` via `DeflatedSystem`, and `_delta_float_gn`) still evaluates Ψ from DEHOMOGENIZED control nets → the top-level witness FAILS for strongly-rational input (Ψ_dehom ≠ 0 at the true touch). NOT a soundness bug: subdivision shrinks cells until the dehomogenized net converges to the rational surface, the small-cell witness converges, and the emitted xyz/stuv is recomputed rationally — measured 4 off-center asymmetric touches (weights to 6×) found to machine precision (st_err ≈ 0). Filed as **L26** (perf/robustness, P2) — fixing DeflatedSystem-Ψ cascades into `_deflate.py`'s shared class (psi_point/psi_box/jac evals, also used by `analyse_deflated_system`), >100 lines. **Audit — Gauss map: SOUND, no fix.** `compute_gauss_map_rational` already applies the quotient rule (A = F_u·W − F·W_u); its numerator direction matches the true rational normal (sin ≤ 1.4e-4) and every true normal (450/450 samples, 30 rational surfaces) lies in the control-direction convex cone → the control-hull separability certificate correctly bounds the rational normal field, cannot falsely certify loop-free.

- [x] **L10. Loop-assembly cluster: off-lattice touch+loop returns 4 out-and-back doubled arcs with ~120° missing** — CLAIMED(main-session) — [B C1 critical + C2 + C6] Partition cuts through the touch bind cells that never trace their arcs; `_assemble_fragments`'s closing-march `gap < 10·median_step` misfires on short arcs of small loops and retraces backwards; thin loops route THROUGH the touch. (Related to L4 — same off-lattice geometry family.) Fix: needs a dedicated pass on assembly's closed-loop path with the off-lattice repro pinned first.
  → FIXED `0ea021e` (five mechanisms, each measured): **(1) grazing-corner seeds** — the primary arc loss was UPSTREAM of assembly: a guided cut TANGENT to the loop (cut through the circle's extremal crossing) leaves a cell whose only registered crossing sits at a box corner with the curve tangent pointing ~1e-9 out of one face; ray-face init exits at α≈0 in the true direction (bounce-killed), the other direction exits through the adjacent face the seed sits on → the cell's whole ~59° arc degraded to an SSXPoint (instrumented: fr=0 pt=1, missing sectors 181–239°/300–358° exactly the cells cut-bounded through the touch). `_trace_cell_by_registrations` attempts 2/3: displace the seed along the curve tangent (α ladder 0.02/0.05/0.1 local), `_ssx_correct` back onto the curve (sin_ang-aware acceptance + in-open-box + >4·ptol progress guards), march from the interior point; the registered crossing is PREPENDED so fragments still start at the registered stuv. **(2) closing-march retraces** — for a <180° arc the end→start chord points BACKWARD across the opening, so the known-endpoint marcher walked back along the traced arc (net angular progress 0.0, logged '9 pts back', ×4 arcs). Pre-guard `path_len > 3·gap` + post-guard rejecting closing segments whose interior samples ALL lie ≤2·atol on the existing polyline. **(3) junction-gap join pass** — the ~1° junction micro-slivers between arcs ARE traced (instrumented: 12+ two-point slivers incl. a genuine two-keeper bridge) but containment dedup eats every one via its terminal-VERTEX clamp (a sample extending a keeper's free end by <2·atol measures as covered); rather than weakening dedup (would resurrect the round-1 skew-duplicate bug), open+interior+tangent-consistent+substantial (arc>16·atol) branch ends within 4·atol are joined post-assembly, junction chords truth-tested (res/max(sin_ang,1e-3) ≤ 2·atol), junctions ≤2·atol from a tangent_point NEVER joined; self-join closes exactly (|s−e|=0). The 16·atol participant floor is load-bearing: without it, eps=1e-3 valley junk self-closed into blobs and frankenjoined onto ring arcs, then containment ate the clean ring (caught by L2's regression test mid-fix). **(4) chain barrier** (C2): transversal fragments never chain through a junction ≤2·atol of an emitted tangent_point (paper semantics: branches terminate at singular points; tangential fragments exempt so traced tangent curves stay whole) — kills the k=64 through-touch V-spike; saddle arms now end at the X (4 arms, coverage unchanged, no branch-count assertion existed). **(5) duplicate-loop collapse + valley-fiction filter** (C6): seeded closed loops march at 0.5·atol sagitta (two 2·atol-sagitta samplings of an aspect-16 cap κ≈160 sat ~3.7·atol apart — outside every 2·atol containment; at 0.5·atol copies stay ≤1·atol), branch-level containment dedup (2·atol, longest-first) collapses the two Φ∩L seed traversals, and branches whose EVERY chord midpoint estimates res/max(sin_ang,1e-3) > 2·atol are dropped (the eps=0.012 touch-to-ring bridge: single chord est 4.6·atol, samples Ψ-valid — all-fiction; genuine branches exit at their first good chord). **Measured:** off-lattice (0.55,0.52) touch+loop: 52·atol worst gap/233 missed → 1·atol/0 missed, 4 fake closed → 1 exact closed loop + 1 tangent_point; eps=0.012 bridge gone (ring+touch only); aniso k=16 two-duplicate-rings+debris → ONE closed ellipse; C5 determinism [3,2,3,2,2,2] → 6× bit-identical (see L17). Tests: `test_offlattice_tangent_point_plus_loop`, `test_aniso_tangent_point_plus_thin_loop`, bridge asserts in `test_repeated_calls_bit_identical`. Gate: 43/43 singular, 7×100% baseline timings, legacy unchanged, 59 unit. **Residuals:** k=64/c=1/150 (10× shallower than the shipped valley test) not re-measured post-fix — the barrier + valley filter address its spike/cap-sliver mechanisms but the stressed corner needs its own pass if it matters; L19's 2.2–4.5·atol debris band unchanged (separate item).

## P1 — contract violations / wrong metadata

- [x] **L11. C3 `branch_links` store broadphase SEGMENT indices, not refined vertex indices** — CLAIMED(bg-session) — `_ssx5_singular.py:742` — [A M10] Umbrella: links point 52·atol away from the actual crossing. Fix: re-anchor links after Newton (nearest vertex on each linked branch to the refined xyz).
  → FIXED `bf22dff`: `_anchor_vertex` — downhill walk from the seeding segment to the locally nearest polyline vertex (a global argmin would collapse both links of a self-crossing branch onto one pass); contract documented on `SSXSingularity.branch_links`. Measured: all umbrella + figure-eight links now nearest-vertex OPTIMAL; the umbrella's 52·atol offset IS the half-chord floor (chords ~0.22 at default density — 4·atol unachievable), so the strengthened tests assert optimality (`_assert_links_nearest_vertex` in all three C3 tests) per the ledger's fallback. Residual: vertex can legitimately sit ~half a chord from xyz; consumers needing sub-chord anchors must use `g.xyz`/`g.stuv` directly.
- [x] **L12. C1 `branch_links` use min-VERTEX distance, not point-to-segment** — CLAIMED(main-session) — `_bez_ssx5.py:4415` — [A m3 ↔ B C10] A cusp exactly ON a coarse branch span gets `links=[]`. Fix: `_dist_point_polyline` + nearest-vertex-of-nearest-segment.
  → FIXED `f2f8497`: `_dist_point_polyline` gate at 4·atol, then anchor at the nearer ENDPOINT of the argmin segment — same vertex contract as L11 (documented there: vertex may sit ~half a chord from xyz). Cusp-edge test's links stay populated; gate 43/43 + harness exit 0.
- [x] **L13. C1 `nscale` mixes homogeneous-net scale (w⁴) with weight-invariant rational residual** — CLAIMED(bg-session) — [B C9] Weight rescale flips cusp detection. Fix alongside L9 (same homogeneous-vs-cartesian hygiene).
  → FIXED `8fc9a43`: `c1_pass`'s GN measures the Σ residual as the CARTESIAN normal Nv = cross(du,dv) (rational eval, weight-invariant) but normalized acceptance by `nscale = max|N|` ~ W⁴ → a uniform weight rescale by c scaled the threshold by c⁴ (B C9). Now normalized by a weight-invariant sampled Cartesian scale (max |cross(du,dv)| over a 5×5 [0,1]² grid, floored); `max|N|` kept as the exact identically-zero gate. Only surfaces passing the Σ-hull precheck AND reaching the newton are affected — coverage cases exit at the precheck (BIT-IDENTICAL; 7×100% unchanged timings), polynomial cusp test unchanged. Test `test_cusp_detection_weight_invariant` (constant ×3 rescale + non-uniform weight net, surface unchanged). **Honest measured behavior:** on the cuspidal-edge geometry the cusp is found at ANY scale in BOTH old and new code (Σ = 0 EXACTLY at the cusp ⇒ `norm(Nv) < tol·scale` holds for any positive scale; precheck excludes non-cusps scale-invariantly) — measured c ∈ 0.003..100, no flip observed on ANY constructed cuspidal-edge/off-center/transversal geometry. The fix is the correct weight-invariant normalization (defense-in-depth for marginal/near-degenerate Σ candidates where the tolerance band decides); the test is a regression guard locking the invariance, not a live-flip reproduction. Could not reproduce the exact B C9 flip on the available cusp geometries within scope.
- [x] **L14. `curve_flag` heuristic (>12 sols) types a genuine cusp CURVE as 3 isolated cusps** — CLAIMED(main-session) — [B C18; partially rooted in L1] Re-measure after L1 lands; then base the flag on collinearity/parameter-span of the solution cloud, not count alone.
  → FIXED (main-session, after `581f7ef`): re-measure CONFIRMED the gray zone is realistic, not sub-resolution — cuspidal edge in plane x=0 clipped to t-extent 0.2 (200× resolution) → 11 sparse sols (xyz-dedup at atol eats intermediates), count ≤ 12, not exhausted → 11 isolated 'cusp's. Also measured: floor-box counting (the collinearity alternative's solver-side cousin) is NON-discriminative for face-aligned curves — dyadic feature coordinates sit exactly on split faces and the descent dies by exclusion with 0 floor boxes on curves vs 16 on the isolated control (inverted!). Shipped: `_connected_one_dim` CONNECTIVITY probe on the 2..12 window — sort along the cloud's principal axis, Newton each consecutive parametric midpoint with the enumeration's own solver; curve ⇒ NEW root inside the pair's AABB distinct from both at 1·ptol; isolated ⇒ dup/divergence; ≥half pairs must connect; ≤2·ptol-adjacent pairs count connected (below resolution = curve by definition). Negative control: du=0 at BOTH s=0.3/0.7 (one surface, two honest isolated cusps) stays 2 isolated — midpoint GN falls back to an endpoint. `solve_zero_dim` gained an optional `stats` out-param ('floor_boxes') from the investigation, kept for diagnostics. Tests: `test_short_cusp_curve_typed_as_curve_not_isolated`, `test_two_isolated_cusps_stay_isolated`. Gate 46/46 + harness exit 0 + legacy + 59 unit.
- [x] **L15. Cone-apex class: Σ=0 degenerate-parametrization contact emits phantom `tangent_point` at a transversal crossing** — CLAIMED(main-session) — `_bez_ssx5.py:537` — [A M3] All four TΨ minors vanish at Σᵢ=0 regardless of tangency; witness doesn't exclude it. Repro: bilinear cone (s·t, s, s(2t−1)) vs z=0 → phantom point at apex where crossing is transversal. Fix: witness acceptance must check actual normal alignment (or Σ≠0) at the converged point; if Σ=0, classify as C1 candidate, not C2.
  → FIXED (main-session, commit after f2f8497): `_normals_degenerate_at` — both C2 emitters reject Δ-roots where either |N_i| ≤ 1e-6·|du_i||dv_i| (reparameterization-invariant ⇒ cell-local nets fine; the 1e-300 absolute floor catches exact-zero derivative products). Classification falls to c1_pass (cusp/cusp_curve + surface tag — the repro now ships exactly {cusp_curve, surface=1} + 1 transversal branch, phantom gone). Healthy touches unaffected (parallel AND nonzero normals). Test `test_cone_apex_no_phantom_tangent_point`; gate 44/44 + harness exit 0 + legacy + 59 unit.
- [x] **L16. C3 dedup ball `4·max(ptol4)` without xyz guard merges distinct C3 points 500·atol apart** — CLAIMED(bg-session) — [B C12] Apply the binding both-guards ladder.
  → FIXED `bf22dff`: module-level `_c3_same_hit` — same hit only if xyz ≤ 2·atol AND unordered preimage pairs match per-axis ≤ 4·ptol4 (either guard failing ⇒ distinct); duplicate re-finds union their links. Measured: the unit test pins the exact failure (ptol4=[0.3,0.3,1e-4,1e-4]: old 6D ball 4·max=1.2 merged pairs 0.35 apart in param at 500·atol xyz separation — new predicate keeps them distinct; swapped-ordering and pair-mismatch cases covered); integration: the figure-eight's two crossings 600·atol apart both ship. Tests: `test_c3_dedup_both_guards` (direct predicate — a slow-axis integration repro needs a near-stationary surface axis, not worth the harness cost given the predicate is exercised by every C3 case), `test_cross_cell_self_intersections_figure_eight`.
- [x] **L17. Nondeterminism: module-global PRNG in the hemisphere-witness search** — CLAIMED(main-session) — [B C5] Identical calls alternate 3/2 branch counts. Fix: seed per-call deterministically (e.g. from the cell box hash) or use a fixed lattice.
  → FIXED `0ea021e`: `_ssx4.reset_witness_rng()` called at every `bez_ssx` entry — reseeds BOTH the numpy `_RNG` (Python fallback path) and the Cython xorshift32 state (`_cap_witness.set_rng_seed(0)`, guarded import). Verifier's root cause confirmed: the Fisher–Yates shuffle in `hemisphere_witness_incremental` consumed cross-call global state, flipping trace-vs-subdivide on marginal near-tangent cells ([3,2,3,2,2,2] by call index on touch_plus_loop(0.012)@5e-3; fresh processes always agreed). A witness miss is completeness-only (found caps are margin-verified) so pinning the shuffle is sound; every call now starts from the fresh-process state, making the first-call behavior THE behavior. Measured: 6 calls bit-identical (branch xyz md5). Test: `test_repeated_calls_bit_identical` (2 calls, byte-compare curves). Residual: within-call shuffle order is now a fixed sequence — an adversarial ordering for the incremental cap search is theoretically constructible but was equally possible at any prior RNG state.
- [x] **L18. Witness chain absolute tolerances lose touches at ×10 coordinate scale** — CLAIMED(main-session) — [B C13] INSIDE the documented O(1)–O(100) envelope, so not covered by the ×1000 accepted limit. Fix: scale-normalize `tol_f`/stall bars by the T-net magnitude (the Risk-6 fix direction, now needed at ×10, not ×1000).
  → RESOLVED BY PRIORS (re-measured at HEAD, no code change): the ×10 loss no longer reproduces — paraboloid/double-touch/saddle scale-covariant through ×100 (1/2/1 tangent points, clean branches/points, coords AND atol scaled). The C13 loss path (verified at 7d47f68) ran through gates fixed since: L4's probe-descent on witness failure foremost (at scale the center witness fails; the old unconditional continue dropped the cell), plus L1 hull margins. New measured envelope boundary: ×300 degrades (1 of 2 touches + spurious branch), ×1000 loses both — OUTSIDE the documented O(1)–O(100) envelope = the pre-existing accepted limit (plan risk 6), now with a sharper boundary number. Known fix direction if the envelope ever needs extending past ×100: normalize the DeflatedSystem/_delta_float_gn INPUTS (center-shift + length-scale on Cartesian nets, per-net max|T| on the minors — zero sets and local-parameter roots invariant, absolute tol_f/stall bars become dimensionless; contained at the three entry points, avoids the L26 cascade). Tests: `test_touch_detection_scale_covariant[10.0/100.0]`.

## P2 — quality / debris / harness

- [x] **L19. Valley-debris SSXPoints at 2.2–4.5·atol from a touch, multiply emitted** — CLAIMED(main-session) — [A m1 ↔ B C15] Point-stack dedup exists since `3b80072` but the 2·atol subsumption radius leaves the 2.2–4.5·atol band; multiplicity partially fixed — re-measure, then either widen with a param guard or accept + document.
  → ACCEPT + DOCUMENT (re-measured at HEAD): multiplicity is GONE (3b80072's point dedup — unique_xyz == count everywhere); the band is down to a SINGLE debris point per case at 2.2–3.1·atol on realistic eps (touch+loop 2e-2 → 1 pt @2.24·atol; 1e-2 → 1 pt @3.13·atol). Widening cannot close it within conventions: the valley debris is param-FAR from the touch (stuv offset ~10× unify_tol — a param-guarded widen never fires) and an unguarded xyz widen to 4–5·atol violates the both-guards ladder and risks eating genuine isolated points. Sub-2.5e-5-eps behavior (7–11 tangent_points at eps ≤ 4e-5) is the documented `_delta_float_gn` stall-bar limit — crossing angle ~2.5e-7 rad, three orders below the validated scale (C16's verifier already judged the class synthetic/minor). Accepted, no code change.
- [x] **L20. Near-tangent transversal valley runtime 2.5–3.7× vs base** — CLAIMED(main-session) — [A m2] Profile `_emit_offcurve_tangent_roots`/probe frequency on the valley geometry; the interval-GN in `_tangency_witness` (Risk-7) is the known hotspot.
  → DOCUMENT + DEFER (measured at HEAD): the gap has GROWN through the ledger's correctness fixes — touch+loop eps=1.2e-2@5e-3 = 46.2 s, eps=5e-3@1e-3 = 58.2 s vs 2.6 s for the plain paraboloid touch (~20×; every tangency-adjacent cell now pays probe descents, off-curve enumeration, Φ∩L seeding rounds and valley filters). Correctness-first accepted per the project owner's explicit direction (runtime not critical; Python reference implementation). Hotspots for a future pass, expected-value order: interval-GN in `_tangency_witness` (Risk-7), Φ∩L seeding re-runs at successive depths of the size-gated descent (cache seeds per touch), `_emit_offcurve_tangent_roots` tube tests (KD-tree cover queries per the DEFECT-D note). No code change in this item.
- [x] **L21. `phi_loop_seeds` cross-plane dedup omits the xyz guard** — CLAIMED(main-session) — [A m5] One-line ladder fix.
  → FIXED `f2f8497`: destructive ladder applied (merge only when 1·ptol/axis AND xyz ≤ atol; seed xyz cached alongside).
- [x] **L22. `c1_pass` computes `surface: 1|2` but wiring drops it** — CLAIMED(main-session) — [A m4] Extend `SSXSingularity` (additive field) or encode in `samples`/doc.
  → FIXED `f2f8497`: additive `SSXSingularity.surface` (None default, documented C1-only semantics); populated at both cusp and cusp_curve emission sites via `hit.get("surface")`.
- [x] **L23. C3 candidate filter 2·atol vs 4·atol worst-case chord-pair gap** — CLAIMED(bg-session) — [A m7] Two chords can pass 2.5·atol apart while true curves intersect (sagitta 2·atol each side). Widen broadphase/seg_dist threshold to 4·atol + slack; cost is broadphase-bounded.
  → FIXED `bf22dff`: seg-pair window 2·atol → 5·atol, broadphase AABB pad 1·atol → 2.5·atol/box (pairwise 5·atol, consistent). The same-preimage guard absorbs the extra near-miss candidates (non-crossing branch pairs converge onto one preimage ⇒ rejected). Measured cost: candidate counts UNCHANGED on both probes (umbrella 3 AABB pairs → 1 window pair at either window; coverage case 10: 0 pairs at either), shipped c3_pass 0.1 ms (umbrella) / 0.3 ms (case 10) — identical to pre-fix; coverage timings ≤1.0× reference, zero new singularities anywhere.
- [x] **L24. Coverage harness zero-singularities expectation is print-only** — CLAIMED(main-session) — [A m8] Turn into an assertion (exit code) so CI catches spurious-singularity regressions.
  → FIXED `f2f8497`: `check_case` returns coverage-ok AND zero-singularities (SPURIOUS SINGULARITIES line printed on violation; no-reference-points early path included); `__main__` already exits 1 on any False.
- [ ] **L25. Edge-graze transversal arc loss** — [A m6] PRE-EXISTING at `40a79a1` (not from this diff); track separately.
- **CLAIM:** L26 — CLAIMED(main-session; promoted into the case-14 P0 root cause before the deferred P2 sweep).
- [x] **L26. Tangency-witness Ψ refinement evaluates DEHOMOGENIZED control nets for rational input** — discovered during L9 (`51f7c4e`) — `_bez_ssx5.py` `_tangency_witness`/`_check_tangency` (via `_deflate.DeflatedSystem`, `P1c = g1.surface[...,:-1]/g1.surface[...,-1:]`) and `_delta_float_gn` (`eval_surface(P1c, rational=False)`). The deflated system's Ψ = R1−R2 is evaluated from the per-control-point dehomogenized nets — the WRONG surface for non-uniform weights (same disease L9 fixed for the T nets, but Ψ here, not the T minors). Consequence measured: the TOP-level witness FAILS for strongly-rational touches (Ψ_dehom ≠ 0 at the true contact). **NOT currently a soundness bug** — the hull PRUNING uses the rational-correct `psi_vector_net`, and subdivision shrinks cells until the dehomogenized net converges to the rational surface, so the small-cell witness converges and the emitted xyz/stuv is recomputed rationally (`_emit_tangent_roots` line 725). Measured: 4 off-center asymmetric rational touches (weights to 6×) all found to machine precision (st_err ≈ 0). **Cost/risk:** the top-level fast-path witness is lost for strongly-rational input → extra subdivision; and a touch held only by a cell at the size-gate floor (4·unify_tol) could be refined slightly imprecisely (bounded by ~ptol, not observed). **Fix (deferred — cascades > 100 lines):** pass homogeneous nets + rational evaluators into `DeflatedSystem` (touches psi_point/psi_box/jac_point/jac_box, shared with `analyse_deflated_system`) and add a `rational` flag to `_delta_float_gn`. Repro: `/tmp` probes in the L9 session — off-center paraboloid `((s−0.3)²+(t−0.7)²)` rationalized w=(1,2,6) makes `_tangency_witness` on the top cell return `ok=False, 0 roots` while `bez_ssx` still finds the touch after subdivision.
  → FIXED (this branch): the tangency witness, Δ refiner, Φ equation selection, and singular trace now consume homogeneous surface nets and evaluate Ψ/Jacobians through the true rational quotient. `_delta_float_gn(..., rational=True)` also independently scales polynomial TΨ equations while validating physical Ψ and normal alignment. Case 14's true rational generator now refines on both surfaces; non-uniform homogeneous scaling and physical-gap controls are pinned by tests.

- [x] **L27. Shared-edge overlap claims: corrupt index-paired endpoints poison branches, crossings, and runtime** — CLAIMED(main-session) — USER-REPORTED (corner-sharing bilinear pair, 2026-07-07): two patches sharing three corners + two whole edges meeting at an exact tangency A. CSX's boundary-overlap claims paired t/u/v range endpoints BY INDEX — garbage for curve-on-surface overlaps (a whole edge "overlapping" one surface point) — and shipped unverified: 3 branches ~39000·atol off the SSI, genuine crossings filtered out by the garbage endpoints (one true branch starved to a 0.142 stub), flat-projection variant orders of magnitude slower.
  → FIXED `5e55b53`: overlap endpoints resolved by point INVERSION (`_invert_point_on_surface`); claims residual-VERIFIED (shipped chord ≤ 2·atol at 5 samples) before acceptance — rejected claims contribute nothing; overlap-curve JUNCTIONS emitted structurally as tangent_points (≥2 distinct OPEN branches, non-collinear, healthy parallel normals, ISOLATED tangency — sin_ang grows past 1e-3 within max(8·atol, 5% arc) along both branches; strips/tangent-rings stay suppressed); F1 endpoint exception scoped to OPEN ends (closed-branch seams still subsumed); tracer collects candidates across all 4 attempts (winner by arc; graze-signature exits — unmatched face exit with tangent ∥ face — keep attempting). SEMANTIC UPGRADE: legacy overlaps case now 2 branches (the TRUE count — the "4-vs-2 pre-existing gap" WAS these corrupt pairings) + its junction tangent_point at (128.25,129.86,0), same feature class as the user's A. User case final: 2 overlap branches residual=0 + 1 tangent_point at A + 0 points, 0.03s; flat variant 0.08s (was minutes). Tests `test_shared_edges_junction_tangent_point[False/True]`; gate 50/50 + harness exit 0 + tangential legacy unchanged + 59 unit. Residual: overlap branches remain 2-point chords (curved genuine overlaps are chord-approximated — pre-existing semantics); junction emission requires the tangency, non-tangent transversal corner junctions of overlap curves emit nothing (none constructed yet).

## New user cases (2026-07-10)

- [ ] **L28. Case 12: coplanar patches partially overlap in a 2D region** — P1 USER-REPORTED (`examples/ssx/bez_ssx5_case12.py`). The current branch/point schema cannot represent the true 2D overlap region and repeated runs have produced different lower-dimensional artifacts. Per Cheng et al. Fig. 8 this is the `#(Δ_B)=∞`, 2-dimensional partial-overlap sub-case. Design the output contract with the user before implementation; candidate contracts include tagged boundary loops, a trimmed-region structure, or paired parameter-space loops.
- [x] **L29. Case 13: rational sphere patch vs bilinear quad never terminates** — CLAIMED(main-session) — P0 USER-REPORTED (`examples/ssx/bez_ssx5_case13.py`). Identify the exact non-terminating loop with instrumentation before changing behavior; add a failing regression, fix the local soundness defect and missing local budget, and verify the global soft-budget fallback.
  → FIXED (this branch): instrumentation showed crossing-less descendants repeatedly paying the same four-plane Φ seed enumeration after the already-certified tangent point had been deduplicated. Ancestor-box attempt caching now makes that search once-per-region, and Φ enumeration/marching spend the shared call budget. The original script terminates with one certified `tangent_point`; its unresolved near-tangent complement is returned explicitly partial rather than falsely complete.
- [x] **L30. Case 14: two rational cone segments never terminate** — CLAIMED(main-session) — P0 USER-REPORTED (`examples/ssx/bez_ssx5_case14.py`). True SSI is one singular tangent branch near degenerate apex rows (`Σ=0` at repeated control points). Identify the exact non-terminating loop with instrumentation before changing behavior; add a failing regression, fix the local soundness defect and missing local budget, and add the global soft-budget fallback without masking the root cause.
  → FIXED (this branch): instrumentation isolated the freeze to `_find_ssx_boundary_zeros → bez_csx → _phase2_isolated_search`: an identically collapsed rational apex edge produced 16,385 pseudo-roots, followed by quadratic dedup and interval splitting. CSX now classifies the positive-dimensional curve-parameter fiber directly, uses a bounded point-on-surface solve, and SSX canonicalizes the two apex fibers before tracing the certified tangent generator. The original script terminates with one tangential branch and honest partial status for the unresolved Δ complement.

## Proactive overlap/rational/no-hang audit (2026-07-10)

- [x] **L31. High-multiplicity regular tangent valleys pass residual-only branch certification** — CLAIMED(regular-touch-redteam) — For the exact family `S1=(s,t,(t-1/2)^d)` vs `z=0`, d=8/10/12 publishes branches 8–48·atol away from the sole SSI line while `|Psi|` is at roundoff scale; d=4 loses the real line. Root cause: a rank-2 Psi Jacobian plus an unhinted 2-D nullspace lets registration tracing choose the singular transverse direction, and residual magnitude is not a root-location certificate at multiplicity d.
  → FIXED (this branch): fixed-face square polishing, a bounded Decimal multiplicity fallback, and a certified partner-direction hint keep the nullspace trace on the exact locus. Degrees 4/8/10/12 each return one tangential line; unresolved multiplicity remains explicit partial.
- [x] **L32. Global SSX budget starts a superlinear 4-D distance-net build before its first charge** — CLAIMED(main-session) — `max_cells=0` still materializes the full squared-distance Bernstein tensor, and the same tensor was built twice. An 16x16 planar pair spent ~9.7 s with reported `cells_processed=0`; larger input can freeze/OOM before the advertised watchdog applies. Preflight the constructor, return immediately at zero allowance, and reuse one net.
  → FIXED (this branch): zero allowance returns before setup, non-disjoint inputs precharge a control-product-squared estimate, and the top distance net is built once then propagated by subdivision.
- [x] **L33. NURBS CCX/CSX adapters reset `max_cells` for every span pair** — CLAIMED(budget-redteam) — 12 mocked CCX pairs at max_cells=3 reported complete after 36 cells; 20 CSX pairs at max_cells=2 reported complete after 40. Maintain one aggregate remaining allowance/result cap and sanitize partial span outputs.
  → FIXED (this branch): both adapters share remaining cell/result allowances across span pairs, stop on exhaustion, discard overproduction, and propagate partial status. Aggregate mock regressions and the full NCSX adapter suite pass.
- [x] **L34. Post-assembly SSX junction/filter scans escape `max_postprocess_work`** — CLAIMED(main-session) — overlap endpoint pairing and later point/branch/C1 scans continued quadratically after exhaustion (1024 synthetic overlap branches: 5.5 s while `postprocess_work=0`). Charge actual scan sizes and stop conservatively on denial.
  → FIXED (this branch): assembly, containment, junction, C1/C3, and final filter scans charge a separate finite call-wide postprocess allowance; denial stops optional scans conservatively while retaining already-certified overlap output.
- [x] **L35. Closest-point degenerate continuation runs outside its cell budget** — CLAIMED(budget-redteam) — with `max_cells=1`, a rank-1 seed consumed the only pop and still entered a 2000-step `trace_equidistant_curve`, reporting complete. Reserve/charge continuation iterations from the same allowance and aggregate NURBS patch budgets.
  → FIXED (this branch): continuation iterations spend the same remaining allowance, truncated traces are never published, and NURBS patch aggregation propagates exhaustion. The 48-test closest-point gate passes.
- [x] **L36. CCX/CSX exactness certificates depend on absolute world translation** — CLAIMED(rational-redteam) — translating a 1e-8 exterior CSX near-root or a 5e-4 offset overlap to large coordinates turns it into complete topology; collapsed fibers have the same absolute-scale defect. Center a common Cartesian origin in homogeneous form before residual/identity scaling. CCX also lacks a source-operation cancellation floor and rejects an exact restricted subcurve.
  → FIXED (this branch): exactness contexts use a shared Cartesian origin in homogeneous form and scale only local motion; affine restriction certificates include a source-operation cancellation floor. Translated real roots survive, while translated exterior roots, false overlaps, and false collapsed fibers are rejected.
- [x] **L37. A regular tracer duplicates a certified overlap subset** — CLAIMED(main-session) — the L3 skew fixture shipped a full `overlap` plus a `transversal` copy of its second half. Remove only with the established same-location stuv AND xyz containment guards; parameter-far sheets must remain distinct.
  → FIXED (this branch): postassembly removes a regular subset only when both interpolated stuv and xyz containment guards hold. The skew fixture now ships the overlap plus its genuine parameter-distinct tangent point, without the duplicate regular half.
- [x] **L38. Fixed 20k nested-CSX scheduling cap makes regular case 11 falsely partial** — CONFIRMED(final-budget-policy review) — internal cut call 86 needed 20,495 cells but 220k of the shared SSX allowance remained; consuming the truncated two-root set would be topology-unsound.
  → FIXED (this branch): topology-critical internal cuts receive CSX's established 100k allowance once, clamped by the remaining shared budget; they never repay a discarded smaller attempt. Independent top-boundary faces retain a separate 20k soft-partial cap. Case 11 completes within a 60k global allowance and returns one nondegenerate closed branch with 16/16 objective coverage.
- [x] **L39. CSX depth 50 stops one level-cycle before a known corner-root remainder reaches resolution** — CONFIRMED(overlap review) — the legacy overlap mini-case returned correct geometry but `incomplete=True`; three parameter axes needed depth 53 to certify the interval next to `(t,u,v)=(1,0,1)` root-free.
  → FIXED (this branch): the bounded CSX default/helper depth is 64 while cell/result budgets remain authoritative. The direct corner fixture completes in 200 cells, and the legacy SSX case is complete with 2 overlap branches plus 1 tangent junction.
- [x] **L40. Tiered internal CSX retry double-spends work and creates avoidable partial results** — CONFIRMED(final-budget-policy review) — case 11 used 69,913 shared cells when a capped 20k attempt was discarded and repeated, versus 49,913 for one established-cap solve; at `max_cells=60_000` the retry policy falsely stopped partial.
  → FIXED (this branch): the two call classes now have separate first-and-only allowances: 20k for independent soft top-boundary probes and 100k for topology-critical internal cuts, both clamped to the shared remainder. The 60k regression is complete and also asserts a nondegenerate closed loop.

## Refuted / already-fixed during review

- Off-curve budget starvation (B C7) — fixed by `3b80072` (skip-exempt budgeting) before/while the reviews ran.
- ×1000-scale branch TRUNCATION — pre-existing core-marcher behavior, bit-identical at base (B C11); the ×1000 tangency-detection limit stays an accepted, documented limit (plan Risk 6) — but see L18 for ×10.
